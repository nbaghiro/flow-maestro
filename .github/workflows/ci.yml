name: CI

on:
  pull_request:
    branches:
      - main
      - develop
  push:
    branches:
      - main
      - develop

jobs:
  test:
    name: Test and Lint
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      # Checkout code
      - name: Checkout code
        uses: actions/checkout@v4

      # Setup Node.js
      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"

      # Install dependencies
      - name: Install dependencies
        run: npm ci

      # Type checking
      - name: Type check
        run: npm run typecheck

      # Linting
      - name: Lint code
        run: npm run lint

      # Format check
      - name: Check code formatting
        run: npm run format:check

      # Run integration tests
      # - name: Run integration tests
      #   run: npm run test

      # Build all packages to ensure no build errors
      - name: Build all packages
        run: npm run build

  # docker-build:
  #   name: Docker Build Test
  #   runs-on: ubuntu-latest

  #   steps:
  #     # Checkout code
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     # Set up Docker Buildx
  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3

  #     # Test backend Docker build
  #     - name: Test backend Docker build
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         file: infra/docker/backend/Dockerfile
  #         push: false
  #         tags: flowmaestro/backend:test

  #     # Test frontend Docker build
  #     - name: Test frontend Docker build
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         file: infra/docker/frontend/Dockerfile
  #         push: false
  #         build-args: |
  #           VITE_API_URL=http://localhost:3001
  #           VITE_WS_URL=ws://localhost:3001
  #         tags: flowmaestro/frontend:test

  #     # Test marketing Docker build
  #     - name: Test marketing Docker build
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         file: infra/docker/marketing/Dockerfile
  #         push: false
  #         tags: flowmaestro/marketing:test
